[project]
name = "website-monitor"
version = "0.1.0"
description = "This is a python based automation project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.33.1",
]
